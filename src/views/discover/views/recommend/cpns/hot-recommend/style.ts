import styled from 'styled-components'

const HotRecommendWrapper = styled.section`
	.recommend-list {
		display: flex;
		justify-content: space-between;
		flex-wrap: wrap;
	}
`

export default HotRecommendWrapper
